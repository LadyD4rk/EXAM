/* Allowed functions: atoi, printf, fprintf, malloc, calloc, realloc, free, stdout,
write */

#include <stdlib.h>
#include <stdio.h>
#include <unistd.h>

int		*create_array(char **av)
{
	
}

int		main(int ac, char **av)
{
	int		*array;
	int		*print;
	
	if (ac < 3)
		return (1);
	array
}